package com.example.backend.models.enums;

public enum NotificationType {
    SUCCESS,    // Green notifications - positive actions
    ERROR,      // Red notifications - failures/errors
    WARNING,    // Yellow notifications - warnings
    INFO        // Blue notifications - general information
}